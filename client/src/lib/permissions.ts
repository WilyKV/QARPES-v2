/**
 * Re-export de shared/permissions.ts — source de vérité unique pour les rôles et permissions.
 * Le client utilise ce fichier via l'alias @/lib/permissions.
 */
export * from "@shared/permissions";
